[project]
name = "ai-automl-analyst"
version = "0.1.0"
description = "AI-powered automated machine learning analysis platform"
readme = "README.md"
requires-python = ">=3.11,<3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["backend"]